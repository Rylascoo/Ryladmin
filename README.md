# Ryladmin

Ryla's **Administrator Runtime + Continuity Authority** for the Ensemble / Kymaean system.

Fresh Administrator chats read `AGENTS.md` first, then `CURRENT_STATE.md`, and re-resolve all live Ensemble authority before acting. `runtime/current/` carries the exact versioned source of the admitted Administrator realization; machine-local `.codex-ensemble` remains generated executable state. If the machine/runtime is missing, use `docs/ADMINISTRATOR_DISASTER_RECOVERY.md` and the executable `recovery/` kit before resuming Administrator execution.

Ryladmin owns Administrator runtime implementation and continuity only. `Rylascoo/Ensemble-Project` remains superior for Administrator policy/security/orchestration law and the single project queue; `Rylascoo/Ensemble-Website` remains Design authority. Ryladmin never creates product, Engineering, Design, provider, validation, experiment, ODR, or backlog authority.
