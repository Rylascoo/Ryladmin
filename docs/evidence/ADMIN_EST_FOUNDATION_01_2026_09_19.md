# ADMIN-EST-FOUNDATION-01 candidate evidence

Status: PARTIAL IMPLEMENTATION / DETERMINISTIC EVIDENCE ONLY / NO CAPABILITY ADMITTED
Owner: Administrator; central queue remains Project Q-ADMIN-04.

Recovered live Project main `9971acd06cabc7e5965b6f4c1a5d65168958ca4c` and Ryladmin
main `005d674f5b303f4f7bec04b5436856057a568d5a`. Read both bootstrap/current-state
surfaces, Project authority, queue, Director establishment decision, quota/write
addendum, orchestration/hygiene/reasoning law and Runtime Specification. Project
exact-main hosted Validation #1076 passed; that is not new native-runtime evidence.

The Director's current request activates bounded preparation in Codex. The isolated
Ryladmin branch is `admin/est-foundation-01`. No child agent, model subprocess,
Reviewer, provider, scheduler, credit reset or new app lease was invoked.

## Deterministic evidence

Candidate implementation is documented in `runtime/foundation/README.md`. Its source
manifest identifies exact module/schema/test/workflow bytes. The adjacent result
JSON records log hashes and test outcomes. Tests cover threshold equality and
hysteresis, mixed/missing/stale windows, reservations, unknown estimates, duplicate
cumulative usage, pause/restart, interrupted writes, exact task/authority/input/tool
identity, protected/foreign/self-grant paths, one writer, read-only roles, excess
depth/concurrency, conflicting arguments, unexpected network/MCP/plugin requests
and invalid/empty/forged results. Rejected writer fixtures assert absence of the
underlying atomic replacement. Allowed writes affect disposable fixture files only.

`runtime/verify-source.ps1 -InstalledRoot ...` passed for all 13 canonical source files.
The installed source/configuration was not changed. This byte comparison does not
prove the executable is available: the recorded Codex executable
`98f7b459ac91593d/codex.exe` is absent. Python 3.12.10 ARM64 runs the offline tests.

## Limitations and unfinished acceptance

- **BLOCKED:** pinned executable missing. Installed-version schema generation,
  authenticated metering/model mapping and actual launch behavior are NOT TESTED.
  The desktop usage read is a separate interface, not pinned-runtime admission.
- **UNVERIFIED:** branch-protection/required-check settings. The GitHub connector
  returned 403 `Resource not accessible by integration`; no credential fallback.
- **NOT TESTED:** OS-enforced writer containment, malicious concurrent path/code
  replacement, live process trees, zero-survivor runtime proof, suspended Reviewer
  repair, disposable runtime reconstruction, independent acceptance and installation.
- The deterministic writer uses coordinator-supplied observations and cooperative
  locks; it is not an authenticated production dispatch boundary. No live writer
  command is exposed. Capability state remains EXPERIMENTAL / NOT_ADMITTED / SUSPENDED.
- Source hashes, schema checks and CI do not promote capabilities. Task token limits
  remain operational metadata; technically enforced token caps are unavailable.
- Self-review only. No independent review or accountable Engineering adoption is claimed.

All paused app work remains held, including Engineer #2's exact producer
`dc1780e46ef999eb01e214671486b83186ca04c4`. No source, lease, queue, validation or
Design authority was changed in Project/Website. Detached evidence trees remain held.

Next owner is Administrator for supported executable recovery/requalification and
remaining FOUNDATION-01 integration/isolation evidence, with accountable candidate
review before merge. The first unfinished package remains FOUNDATION-01.
