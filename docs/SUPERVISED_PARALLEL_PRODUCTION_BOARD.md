# Ensemble Supervised Parallel Production Board

Status: operational Administrator continuity board only. This file does not create Engineering, Design, product, provider, validation, or automation authority. Live owning repositories and their exact active refs always win.

Updated: 2026-09-12

## Purpose

Use available Codex and Claude subscription capacity in parallel before C11 automation is commissioned, while preserving one-writer-per-lane discipline, exact-ref review, and fail-closed integration.

This board is a supervised dispatch surface, not an autonomous scheduler. Manual Claude reviews under the commissioned C9 review plane do not satisfy C9A, C10, or C11 and must not be represented as automated routing.

## Reconciled operating snapshot

- Ryladmin live reconciliation before this result branch: `4c93bb3d98c61996c81c92899542a8186903bb99`.
- Project `main`: `5a0709233c9f897584f8bff849c83db354ac45c1`; checked-in `CURRENT_STATE.md` still says Run 01 READY/unconsumed, but Administrator-verified local terminal evidence now proves the reserved namespace is consumed. Owning reconciliation is routed through Project Issue #103.
- Run 01 `E0B-Q01-MIX-20260912-01` sealed terminal `TechnicalFailure` after 2 accepted turns; turn 3 WREN Performer failed `gemini-http-503;status=UNAVAILABLE`. Estimated spend is `$0.17175080`, failed-attempt provider usage is unknown, all 24 seal-listed artifacts rehash cleanly, and no matching process remained at census. No retry/replay/fallback is authorized.
- Website `main`: `59f6d429cc02a70e019da4eb112c9b8c9f727f38`.
- No active serial Design branch exists. SEL-S1 result head `108e9b5ecdbed3cdee7f95ac12979e4322286b7a` is archived and its remote source branch is retired. **F4 - Shallow Ground Arc** remains the sole provisional survivor. Exact next is a fresh-serial bounded exact-incumbent Stage comparison method; `PKT-STAGE-CORE-02` remains unchanged.
- Claude Code commissioned manual-review realization: `2.1.267`, dedicated `C:\Users\Wiryl\.claude-ensemble`, claude.ai first-party Pro subscription boundary.
- Ambient Codex is `0.154.0`; the commissioned Administrator runtime remains pinned separately and C10 stays source-disqualified/blocked on 0.154.0.

Re-resolve all relevant refs immediately before every dispatch. Any movement invalidates an exact-ref packet until rebuilt.

## Dispatch law

1. Exactly one writer owns each active Engineering or Design worktree. Reviewers do not write that worktree.
2. Claude is advisory only. Use fixed exact-ref packets outside repositories, no repository working context, no tools/MCP/subagents/session persistence, no API-key/cloud-provider billing route, and no automatic retry.
3. Codex implementation workers operate only in isolated owning-lane worktrees and obey the owning repository's `AGENTS.md`, `CURRENT_STATE.md`, authority, validation, branch, and shared-Git laws.
4. No manual review or worker output changes authority until the owning Sol/Administrator verifies and durably adopts it through normal repository process.
5. Provider traffic/spend, ODR resolution, Design taste, validation promotion, automatic merge, and automatic unique-branch deletion remain outside this board unless separately authorized by their owning authority.
6. Project `docs/PROJECT_EXECUTION_QUEUE.md` is sequencing only. Website `CURRENT_STATE.md` controls current Design execution when the Project queue is stale.

## Parallel board

| ID | Status | Primary executor | Mode | Exact current target | Deliverable / stop condition |
| --- | --- | --- | --- | --- | --- |
| PP-ENG-01 | RUN 01 CONSUMED / TERMINAL TECHNICALFAILURE / OWNING CLOSEOUT PENDING | Engineering Sol / Evidence via Issue #103 | sealed evidence audit + Q-E0B-01 closeout only | Project `main@5a070923...`; RunId `E0B-Q01-MIX-20260912-01`; local seal `1F55B01A...`; executable `d1073fe2...` | Namespace is consumed. Terminal after 2 accepted turns; turn 3 provider 503/UNAVAILABLE; 24/24 listed artifacts hash-clean. Preserve/seal/audit/ledger and determine owning closure/successor. No retry, replay, fallback, replacement RunId, scoring of a non-12/12 run, or further provider traffic. |
| PP-ENG-CLAUDE-01 | PARTIAL-COMPLETE / NO ADOPTION | Director-triggered Claude manual review | read-only independent review | exact Project `main@ba02d168...` plus candidate `d1073fe2...` | Turn completed normally but violated the required output contract: preliminary observations only, no final disposition or severity-ranked findings. Partial observations were independently verified; no Engineering change was adopted. Durable record: `docs/PP_ENG_CLAUDE_01A_PARTIAL_RESULT_2026_09_12.md`. Do not auto-retry the packet. |
| PP-DES-01 | F4 EXACT-INCUMBENT STAGE COMPARISON METHOD NEXT | Design Sol / Codex | fresh bounded Stage comparison method | Website `main@59f6d429...`; archived SEL-S1 result `108e9b5...` | No active serial exists. Open one fresh serial and freeze a bounded exact-incumbent Stage comparison method using F4 - Shallow Ground Arc as the sole challenger while preserving every other `PKT-STAGE-CORE-02` variable. No F4b/reroll/retune or implementation authority. |
| PP-DES-CLAUDE-01 | HOLD - STAGE COMPARISON METHOD FIRST | Director-triggered Claude manual review | read-only Design audit | current Design authority `main@59f6d429...` | No active serial exists, but the next Stage-comparison method is not yet frozen. Build no review packet until Design materializes a new exact reviewable boundary; then commission a new exact-ref packet only if independent review adds material value. |
| PP-ADMIN-C9A-R3 | HOLD - E0-B TERMINAL RECONCILIATION NOW PRECEDES R3 DISPOSITION | Engineering Sol via Issues #103 then #100 + Administrator | Project decision evaluation only after fresh owning Q-E0B-01 closeout | Project Issues #103 and #100; Project `main@5a070923...`; re-resolve Ryladmin before action | Run 01 has consumed its namespace and sealed terminal `TechnicalFailure`; checked-in Project state has not yet reconciled that outcome. Issue #100 was updated to require fresh recovery after #103. No R3 candidate implementation, live invocation, retry, or commissioning authority exists. |
| PP-XLANE-CLAUDE-01 | TERMINAL TIMEOUT / NO CLAUDE RESULT / ADMIN FINDINGS ROUTED | Director-triggered Claude manual review + Administrator verification | one sealed read-only contradiction audit; no retry | sealed packet: Project `main@5a070923...` + Website `main@1f0b696e...`; post-run Website races: `108e9b5...` -> `1953bf7...` -> `59f6d429...`; packet `B383AE2B...` | Single Claude turn timed out fail-closed at 180 s with zero stdout/stderr. No Claude finding was adopted. Before Ryladmin promotion, Website moved through SEL-S1 promotion/closeout; Administrator re-read each material boundary, verified the stale Project-ref defect still persisted at `59f6d429...`, refreshed Website #56 / Project #102, and preserved the packet baseline separately from current Design state. |
| PP-CODEX-DESKTOP-01 | READ-ONLY PASS / MUTATING PILOT READY | Director-operated Windows Codex desktop | supervised fixed-packet analysis; future edit/test pilot | Administrator-created packet/worktree; `docs/CODEX_DESKTOP_SUPERVISED_WORKER_ADMISSION_2026_09_12.md` | `PP-CODEX-DESKTOP-RO-01` passed: complete E0-B activation-readiness audit, useful epistemic discipline, frozen packet remained byte-identical. Further read-only packets are admitted. A mutating pilot must still take the lane's sole writer slot on an Administrator-created worktree and obey the no-Git/no-network/no-provider boundary. |
| PP-CODEX-DESKTOP-RO-02 | COMPLETE / VERIFIED PASS | Director-operated Windows Codex desktop | read-only C9A R3 adversarial audit | sealed packet `CEE19119...`; manifest `7FF63923...`; result `docs/PP_CODEX_DESKTOP_RO_02_RESULT_2026_09_12.md` | Full report returned `R3_HYPOTHESIS_NOT_ISOLATED`; Administrator independently verified 24/24 packet artifacts and consequential findings. Read-only pilot passes; result creates no C9A or mutating-worker authority. |
| PP-C10-UPSTREAM | BLOCKED | Administrator research | capability watch | future Codex release/source | Reopen only after exact source/release inspection proves `PreToolUse` hook-process failure can block underlying dispatch and Project/Director commissions the exact candidate. 0.154.0 remains ineligible. |

## Recommended concurrency now

PP-ENG-01 and PP-DES-01 remain the only owning-lane writer slots. Engineering's active responsibility is now the sealed E0-B Run 01 terminal audit/closeout routed through Issue #103; do not retry the consumed run or let Issue #100 R3 work preempt that reconciliation. Issue #100 remains open but its R3 disposition must fresh-recover after #103. Design current authority at Website `main@59f6d429...` has no active serial in `CURRENT_STATE`; a fresh Stage-comparison branch exists at the same main commit but has not diverged, so Administrator does not infer Design authority from its name. F4 remains provisional and the next owning Design gate is the bounded exact-incumbent Stage comparison method. PP-CODEX-DESKTOP-RO-02 is terminal and verified. A future mutating desktop pilot still requires explicitly taking an owning lane's sole writer slot on an Administrator-created isolated worktree. PP-ENG-CLAUDE-01A and PP-XLANE-CLAUDE-01 are terminal/non-retryable. Hold PP-DES-CLAUDE-01 until Design materializes a new exact reviewable boundary.

Do not add a second writer to either owning lane merely to consume quota. Extra capacity should go to independent review, falsification, test-gap analysis, or future-work preparation.

## Manual Claude review contract

Before each review, verify `CLAUDE_CONFIG_DIR=C:\Users\Wiryl\.claude-ensemble`, claude.ai / first-party subscription auth, and absence of `ANTHROPIC_API_KEY` or cloud-provider routes. Build the packet deterministically from exact refs into Administrator/Claude scratch outside all repositories. Record packet SHA-256 and byte count.

Run one bounded review turn with built-in tools, MCP, subagents, permission prompting, and session persistence disabled. Capture the actual Claude version/model/provider telemetry, turn count, terminal reason, web-search/web-fetch count, output bytes, and output SHA-256. A timeout/error is evidence, not permission to auto-retry.

Expected result schema: overall `PASS`, `PASS_WITH_FINDINGS`, or `FAIL`; ordered findings with severity; exact evidence references; uncertainty/unsupported claims; recommended owning-lane verification; explicit statement that the review created no authority.

## Integration rule

The Administrator or owning Sol independently verifies every actionable finding against the exact owning ref. Adopt only verified corrections. If the implementation/design ref moved after packet construction, rebuild the packet rather than applying stale advice.

## Successor automation boundary

Supervised parallel production is intentionally useful before C9A/C11 succeed. It accumulates real evidence about packet size, review latency, useful task classes, failure modes, and reconciliation cost. Those measurements should inform a future materially changed C9A realization and later C11 automation, but this board itself does not unlock either gate.
