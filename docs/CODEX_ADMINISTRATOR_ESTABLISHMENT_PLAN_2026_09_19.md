# Codex Administrator Establishment - Implementation Entry

Status: APPROVED PROGRAM IMPLEMENTATION PLAN / NO LIVE CAPABILITY ADMITTED BY THIS FILE
Queue owner: Ensemble-Project / Q-ADMIN-04

## Owning authority

Read live `Rylascoo/Ensemble-Project/docs/PROJECT_CODEX_ADMINISTRATOR_ESTABLISHMENT_DIRECTOR_AUTHORIZATION_2026_09_19.md` through EOF after normal bootstrap. That Director decision, not this plan, grants standing establishment authority. Project remains the only backlog and owns policy; Ryladmin owns executable runtime and continuity.

This plan translates the approved objectives into deliverables; it is not an alternative queue. No Codex process, native subagent, provider request, Reviewer trial, or runtime installation was performed to create this entry.

## First Codex session

Open Ryladmin in a read-only Codex context with subagents/automatic continuation disabled until admission is verified. Read AGENTS.md and CURRENT_STATE.md, fresh-resolve owning refs, and verify the local runtime against source before executing anything. Do not infer that a newer ambient Codex executable is admitted.

The existing Administrator/Worker launchers admit Project and Website lanes, not Ryladmin. Establish a tested Ryladmin-aware entrypoint in an isolated candidate; do not pass Ryladmin through an unrelated lane or change the active installation to make bootstrap succeed. Initial implementation uses a Director-started, explicitly bounded isolated worktree; no unverified child is needed to build the first safeguards.

Program activation in Codex authorizes routine internal work and deterministic tests. First live trials require the exact sealed successor opening and all conditions in the owning Director decision. No additional blanket approval is needed for steps already covered there. Missing app/OS/account permission remains a real access blocker.

## First bounded work package: ADMIN-EST-FOUNDATION-01

Objective: one dependable authority-recovery and dispatch-admission path before any new agent is allowed to run.

Owned surfaces: Ryladmin AGENTS.md/current continuity, runtime verifier, candidate launcher/configuration sources, versioned capability/task/result schemas, deterministic tests, and repository CI. No Project Product source, Website assets/law, installed `.codex-ensemble`, credentials, or paused worktree mutation.

Implement together:
- repository CI for source integrity, configuration parsing, source/private-state exclusion, schema validation and deterministic negative fixtures;
- explicit capability states ADMITTED / SUSPENDED / EXPERIMENTAL / NOT_ADMITTED, grounded in exact Project authority and validated implementation;
- dispatch/result identity, exact-ref/path/tool allowlists, hash validation, bounded execution/usage settings and cancellation ownership;
- argument validation rather than unrestricted permission overrides;
- the smallest Ryladmin-aware launcher needed to use those checks;
- a compact resumable checkpoint and one evidence summary.

Tests must reject stale/wrong HEAD, unknown capability, changed authority/input, protected or foreign write surface, excess depth/concurrency, conflicting arguments, unexpected network/MCP/plugin startup and invalid/empty result fields. Assert both rejection and absence of underlying dispatch. Keep unsupported token enforcement clearly distinct from observed usage accounting.

Set up branch protection/required checks only through supported authorized administration access; do not retrieve tokens or bypass access if the connector cannot configure settings. A missing protection setting must remain explicitly unverified, not declared completed because YAML exists.

Return: exact reviewed candidate/PR, deterministic result manifest, remaining limitations, and next admitted step. Merge through explicit PR operations; never generic contents writes to main. No source-verifier PASS may be used as a claim of process containment or native capability admission.

## Following deliverables, within the same program

**Containment and recovery.** Repair Reviewer supervision outside the installed runtime. Bound parent/descendant lifetime, stdin delivery and output collection; preserve partial output and terminal status under every failure. Use typed result validation rather than marker presence. Test normal exit, wrapper exit with live descendants/held pipes, timeout, crash and supervisor death. Require zero owned survivors. Preserve old evidence and never invoke the suspended supervisor as the independent reviewer of its own fix.

Unify reconstruction with canonical runtime source plus non-secret machine bindings. Repair the recovery sentinel mismatch, exact tool/path/config verification, restored-launcher test, idempotence and rollback. Rehearse in a disposable profile, not the active user runtime. Historical recovery templates must not overwrite the surviving installation.

**Agent admission.** Follow the owning decision's new sealed MA-01 successor conditions. Start with read-only Scouts; then the repaired Reviewer/MA-02; assess asymmetric writer permissions separately. A failed native inheritance test falls back to an independently admitted top-level Worker, not permission broadening. Keep old failed run identities consumed and create no silent retries. This plan does not itself instantiate or enable any profile.

**Measured delivery.** Use one real authorized task, not a synthetic token burn. Reconcile exact sources, validated results, aggregate usage where available, elapsed time, corrections and user-visible outcome. Claim no efficiency percentage without measurements. No requirement keeps all roles busy.

## Functional roles and efficient context

One Administrator is the Director-facing coordinator. Engineering Sol remains accountable for engineering acceptance. Task profiles can specialize in Application, Persistence, Windows/runtime, reconnaissance and independent review. Instantiate only needed roles; initial ceilings are one executor by default, at most two delegated jobs in total, depth one, and no recursive spawning. Two writers need independently proven isolation and nonoverlapping leases.

Use targeted exact-ref packets, compact evidence summaries, deterministic mechanical checks and artifact references. Read all mandatory authority; reuse an unchanged exact baseline and retrieve deltas rather than replaying the chat/history. Escalate model/effort for real ambiguity or consequential risk, not routine Git/CI reporting. Never hard-code volatile model names into Product architecture.

## Paused-work and app-delivery handoff

Other chats remain paused. Before later app work, resolve current Project lease/PR states and obtain a recorded ownership transfer for any work being taken over. Preserve all uncommitted and exact-head evidence; no new agent shares a write grant with an old paused chat.

At plan creation, Project #223 returned PR #226 at `dc1780e46ef999eb01e214671486b83186ca04c4`; it was not integrated. This is a snapshot, not the next Codex task. #225 remains an interface request. Recheck all live refs before later Engineering Sol acceptance. Do not open new Persistence/Windows mutation merely because the runtime is commissioned.

After explicit app resume, prefer complete creator operations with tests across Application, persistence, Windows and Design. Carry forward the audit's source-derived cold-start recovery and post-start I/O handling gaps for reproduction, and consume the exact current Design return from Website #107. Do not redesign the Product or website from Administrator authority.

## Completion and resume

Establishment is not complete until a fresh session can recover authority, execute an admitted bounded task, reject forbidden actions, seal a valid terminal result with zero owned survivors, and leave resumable state. General unattended scheduling, provider spend, publication, Store submission, and unrelated task pickup remain separately gated.

Minimal future resume:

```text
Resume the Administrator from live Rylascoo/Ryladmin authority in Codex. Read AGENTS.md and CURRENT_STATE.md, recover Project Q-ADMIN-04 and its Director authorization, then continue the first unfinished establishment package. Keep paused app work untouched and unadmitted capabilities disabled.
```
