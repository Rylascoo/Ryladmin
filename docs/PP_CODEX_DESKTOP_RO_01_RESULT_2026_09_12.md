# PP-CODEX-DESKTOP-RO-01 Result — 2026-09-12

Status: **READ-ONLY PILOT PASS — CONTENT USEFUL; NO MUTATING-WORKER CLAIM YET**

This record preserves the first supervised Windows Codex desktop worker result. It creates no Engineering, Design, provider, validation, product, ODR, deployment, scheduling, background-execution, C9A, C10, or C11 authority.

## Frozen input

Task: independent E0-B live-activation readiness audit.

Exact Project authority ref: `8901ea80089998fb6d4f15ac82057ba8f41becce`.

Packet root: `C:\Users\Wiryl\.codex-ensemble\scratch\parallel-production\pp-codex-desktop-ro-01`.

Packet SHA-256: `49850EA63870A5208C04E34599A83C2E517A68B6F2A16DB4D8DDD04019B0A274`.

Manifest SHA-256: `F32C190215BD231609C583A49A7745A217D9D1EA62FA894986E034E79FEFD655`.

Frozen source set: 20 files, 186,621 source bytes. Post-run Administrator verification found all 20 files still byte/hash-identical to the manifest, no `.git` directory, and no extra files.

Returned report SHA-256: `4C5C31418C7ACF1408F3BDBD3CB6EEC76A392301A56182ACFFA07EEBB5C8180A` (23,110 bytes as supplied back to the Administrator).

## Worker disposition

Codex returned `READY_TO_DRAFT_ACTIVATION`, explicitly limited to drafting rather than provider execution.

The report correctly identified the major activation bindings: exact native executable/tag, fresh RunId and nonexistent root, canonical Missing Raft fixture, fixed mixed cast, inherited 12-turn/zero-retry/no-fallback envelope, USD 5 ceiling, dual-route quota/rate/pricing provenance, protected credential/account readiness, blind-scoring freeze, evidence identities, and terminal-consumption law.

It also correctly surfaced the executable provider-fact freshness cutoff of 2026-09-14 and treated current provider/account capacity as execution-time evidence rather than inferring it from prior runs.

The report found no material contradiction among Project `CURRENT_STATE.md`, queue, Director decision, implementation audit, native validation, and integration closeout.

## Administrator verification

The consequential technical claims were independently checked against the exact frozen ref and matched source authority, including:

- native authority `d1073fe2c76e2e05f2daac47465f86b48b456a9a`, validation tag and tag object;
- PR #96 merge `8b3e22bbbeaa0004029b007b86e9b504858a7f41` and post-merge Validation #719 PASS;
- VOSS-only 3.1 recast with MARLOWE/WREN/Integrity/Interpreter retained on the 3.5 reference route;
- 15 RPM / 250,000 input TPM / 500 RPD route assumptions, conservative shared-project minimum pacing, and paid shadow rates encoded for 3.5 and 3.1;
- pre-transcript blind scoring requirement and the separate exact live-run activation requirement;
- `rateSnapshotVerifiedOn = "2026-09-12-pre-run-refresh-required"` and the 2026-09-14 fail-closed pricing/data-use freshness limit.

One severity item is packet-limited rather than a live defect. Codex classified the absence of an exact E0-B run authorization as HIGH because the frozen packet omitted `docs/evidence/GEMINI_API_PROJECT_RELEVANCE_STANDING_DIRECTOR_AUTHORIZATION_2026_09_10.md`. Fresh live reconciliation shows that record remains ACTIVE and grants standing protected Gemini-key use for materially relevant already-authorized Free-tier project work, while preserving per-run exact identity, no-retry, synthetic-only, usage-ledger, and provider-policy boundaries. Project `CURRENT_STATE.md` still blocks E0-B provider traffic until the exact activation is durable. Therefore no new defect is created by the packet omission.

Codex appropriately labeled missing material as unknown rather than inventing it. Its packet-limitations section is therefore evidence of useful epistemic discipline, not a failure.

## Pilot verdict

`PP-CODEX-DESKTOP-RO-01`: **PASS** for read-only supervised analysis.

Pass basis:

- complete final deliverable rather than preliminary observations;
- correct authority-versus-execution distinction;
- useful source-specific findings and ordered activation checklist;
- no discovered mutation of the frozen packet;
- no unsupported adoption into owning Project authority;
- packet omissions were surfaced as unknown/gaps rather than silently filled.

This PASS admits further read-only desktop Codex packets under the same supervised boundary. It does **not** yet commission desktop Codex as a mutating worker. The first mutating worktree pilot remains separately required under `docs/CODEX_DESKTOP_SUPERVISED_WORKER_ADMISSION_2026_09_12.md`.


## Post-pilot live reconciliation

Before this result was committed, Project `main` advanced to `52bd7a934d9d9d48ebf82741b4c495030a746cbc`. Engineering independently performed the exact progression the worker identified as safe drafting work: reserved fresh Run 01/root, froze the blind scoring instrument, rechecked public route facts, preserved exact native executable authority, and stopped before provider traffic. The remaining live gate is protected authenticated AI Studio project/tier/quota/capacity evidence for both 3.5 and 3.1 routes, followed by durable exact preexecution activation. This convergence strengthens the assessment that the read-only worker output was project-relevant and correctly sequenced.
